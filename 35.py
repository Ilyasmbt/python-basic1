#split string
# the split() method returns a list where the text between
#the specified seperator becomes the list items.


#the split() method splits the string into substrings
#if it finds instance of the separator:

a="hello-world"
print(a.split("-j"))